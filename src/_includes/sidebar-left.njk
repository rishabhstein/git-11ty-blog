<h3>Blogroll</h3>
{% include "blogroll-snippet.njk" %}

<p>
  <a href="/guestbook/" class="sidebar-guestbook">
    <img src="/assets/images/guestbook.gif" alt="" class="sidebar-guestbook-gif">
    <span class="sidebar-guestbook-text">Sign my<br>Guestbook</span>
  </a>
</p>
{# postrollItems / bearblogDiscoveryItems are async shortcodes: they must be
   called by the template using this include, not from inside it — async
   shortcodes silently abort the render when this file is included from a
   layout (base.njk). #}
